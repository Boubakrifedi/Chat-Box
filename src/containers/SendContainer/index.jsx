import CustomInput from "../../components/CustomInput";
import CustomButton from "../../components/CustomButton";
const SendContainer = () => {
  return (
    <footer>
      <CustomInput placeholder="Type your message" />
      <CustomButton text="Send" />
    </footer>
  );
};

export default SendContainer;
